# Umbra Deployment Credentials — Example Template
# =============================================================================
#
# Copy this file to .deploy-credentials and fill in your actual values:
#   cp .deploy-credentials.example .deploy-credentials
#
# IMPORTANT: .deploy-credentials is gitignored and will NOT be committed.
# =============================================================================

# SSH Configuration
# -----------------
# The SSH user for connecting to your servers
SSH_USER="root"

# Path to your SSH private key (optional - uses default SSH config if not set)
# Supports ~ for home directory
SSH_KEY_PATH="~/.ssh/id_rsa"

# Frontend Deployment (chat.umbra.chat)
# -----------------------------------------------
# The hostname for the frontend server
FRONTEND_HOST="chat.umbra.chat"

# Absolute path on the server where the frontend files should be deployed
# This should be the web root served by nginx/apache
FRONTEND_PATH="/var/www/chat.umbra.chat"

# Relay Server Deployment (relay.umbra.chat)
# ----------------------------------------------------
# The hostname for the relay server
RELAY_HOST="relay.umbra.chat"

# Absolute path on the server where the relay Docker setup lives
RELAY_PATH="/opt/umbra-relay"

# Docker Registry (Optional)
# --------------------------
# If you want to push Docker images to a registry instead of building on server
# Uncomment and fill these in:
# DOCKER_REGISTRY="registry.example.com"
# DOCKER_USERNAME="your_docker_user"
# DOCKER_PASSWORD="your_docker_password"
