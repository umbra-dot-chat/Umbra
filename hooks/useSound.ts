/**
 * useSound — Convenience re-export of the sound context hook.
 *
 * Usage:
 *   const { playSound } = useSound();
 *   playSound('message_send');
 */

export { useSound } from '@/contexts/SoundContext';
export type { SoundContextValue } from '@/contexts/SoundContext';
